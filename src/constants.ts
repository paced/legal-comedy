/**
 * The selector for the parent of all Tweets.
 */
export const SELECTOR_FOR_TWEETS_CONTAINER = "main div[tabindex='0'] div section div div";

/**
 * The selector for the element that contains children that change when a page navigation happens.
 */
export const SELECTOR_FOR_PAGE_NAVIGATION = "main div";

/**
 * The selector for the highest static element that contains all Tweets on the page.
 */
export const SELECTOR_FOR_ROOT_STATIC_ELEMENT = "div[id='react-root'] div div";
